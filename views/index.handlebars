<div id="home">
  <div class="container">
    <div class="row">
      <div class="col-8 offset-2">
        <h1>{{msg}}</h1>
        <h2>This is our Home Page!</h2>
        <form class="clearfix mb-4" action="POST">
          <div class="form-group">
            <label for="drink-text">Drink Text</label>
            <input type="text" id="drink-text" class="form-control" aria-describedby="drink-text" placeholder="Enter Drink Text">
          </div>
          <div class="form-group">
            <label for="drink-description">Drink Description</label>
            <textarea class="form-control" id="drink-description" aria-describedby="drink-description" placeholder="Enter Drink Description"></textarea>
          </div>
          <button id="submit" class="btn btn-primary float-right">Submit</button>
        </form>
        <h3>Drinks:</h3>
        <ul id="drink-list" class="list-group">
          {{#each drinks}}
          <li data-id="{{this.id}}" class="list-group-item">
            <a href="drink/{{this.id}}">{{this.text}}</a>
            <button class="btn btn-danger float-right delete">ｘ</button>
          </li>
          {{/each}}
        </ul>
      </div>
    </div>
  </div>
</div>

<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="/js/index.js"></script>
